diff --git a/server/BandwidthController.cpp b/server/BandwidthController.cpp
index 8a96738..477972d 100644
--- a/server/BandwidthController.cpp
+++ b/server/BandwidthController.cpp
@@ -36,6 +36,7 @@
 #include <sys/stat.h>
 #include <sys/types.h>
 #include <sys/wait.h>
+#include <unistd.h>
 
 #include <linux/netlink.h>
 #include <linux/rtnetlink.h>
@@ -43,6 +44,7 @@
 
 #include "android-base/stringprintf.h"
 #include "android-base/strings.h"
+#include "android-base/properties.h"
 #define LOG_TAG "BandwidthController"
 #include <cutils/properties.h>
 #include <log/log.h>
@@ -169,6 +171,22 @@ static const std::vector<std::string> IPT_FLUSH_COMMANDS = {
 
 static const uint32_t uidBillingMask = Fwmark::getUidBillingMask();
 
+static bool isWisdomLegacyKernelCompat() {
+    return android::base::GetProperty("ro.boot.hardware", "") == "exynos7904" ||
+           android::base::GetProperty("ro.hardware", "") == "exynos7904" ||
+           android::base::GetProperty("ro.product.device", "") == "wisdom" ||
+           android::base::GetProperty("ro.product.device", "") == "p205" ||
+           android::base::GetProperty("ro.lineage.device", "") == "wisdom";
+}
+
+static bool legacyKernelWithoutNetdBpf() {
+    return isWisdomLegacyKernelCompat() &&
+           (access(XT_BPF_INGRESS_PROG_PATH, F_OK) != 0 ||
+            access(XT_BPF_EGRESS_PROG_PATH, F_OK) != 0 ||
+            access(XT_BPF_ALLOWLIST_PROG_PATH, F_OK) != 0 ||
+            access(XT_BPF_DENYLIST_PROG_PATH, F_OK) != 0);
+}
+
 /**
  * Basic commands for creation of hooks into data accounting and data boxes.
  *
@@ -201,6 +219,38 @@ static const uint32_t uidBillingMask = Fwmark::getUidBillingMask();
  */
 
 std::vector<std::string> getBasicAccountingCommands() {
+    if (legacyKernelWithoutNetdBpf()) {
+        // SM-P205/wisdom is brought up with a legacy 4.4 kernel that has no
+        // complete netd BPF program set. Keep netd alive with the
+        // non-BPF bandwidth chains; detailed UID accounting remains disabled.
+        return {
+                "*filter",
+                "-A bw_INPUT -j bw_global_alert",
+                "-A bw_INPUT -p esp -j RETURN",
+                StringPrintf("-A bw_INPUT -m mark --mark 0x%x/0x%x -j RETURN", uidBillingMask,
+                             uidBillingMask),
+                StringPrintf("-A bw_INPUT -j MARK --or-mark 0x%x", uidBillingMask),
+                "-A bw_OUTPUT -j bw_global_alert",
+                "-A bw_costly_shared -j bw_penalty_box",
+                "-A bw_penalty_box -j bw_happy_box",
+                "-A bw_happy_box -j bw_data_saver",
+                "-A bw_data_saver -j RETURN",
+                "COMMIT",
+
+                "*raw",
+                StringPrintf("-A bw_raw_PREROUTING -m mark --mark 0x%x -j DROP", CLAT_MARK),
+                ("-A bw_raw_PREROUTING -i " IPSEC_IFACE_PREFIX "+ -j RETURN"),
+                "-A bw_raw_PREROUTING -m policy --pol ipsec --dir in -j RETURN",
+                "COMMIT",
+
+                "*mangle",
+                ("-A bw_mangle_POSTROUTING -o " IPSEC_IFACE_PREFIX "+ -j RETURN"),
+                "-A bw_mangle_POSTROUTING -m policy --pol ipsec --dir out -j RETURN",
+                StringPrintf("-A bw_mangle_POSTROUTING -j MARK --set-mark 0x0/0x%x",
+                             uidBillingMask),
+                COMMIT_AND_CLOSE};
+    }
+
     // clang-format off
     std::vector<std::string> ipt_basic_accounting_commands = {
             "*filter",
diff --git a/server/Controllers.cpp b/server/Controllers.cpp
index 48d0c50..39d01a2 100644
--- a/server/Controllers.cpp
+++ b/server/Controllers.cpp
@@ -15,12 +15,15 @@
  */
 
 #include <cinttypes>
+#include <errno.h>
 #include <regex>
 #include <set>
 #include <string>
+#include <unistd.h>
 
 #include <android-base/stringprintf.h>
 #include <android-base/strings.h>
+#include <android-base/properties.h>
 #include <netdutils/Stopwatch.h>
 
 #define LOG_TAG "Netd"
@@ -53,6 +56,19 @@ namespace {
 static constexpr char CONNMARK_MANGLE_INPUT[] = "connmark_mangle_INPUT";
 static constexpr char CONNMARK_MANGLE_OUTPUT[] = "connmark_mangle_OUTPUT";
 
+static bool isWisdomLegacyKernelCompat() {
+    return android::base::GetProperty("ro.boot.hardware", "") == "exynos7904" ||
+           android::base::GetProperty("ro.hardware", "") == "exynos7904" ||
+           android::base::GetProperty("ro.product.device", "") == "wisdom" ||
+           android::base::GetProperty("ro.product.device", "") == "p205" ||
+           android::base::GetProperty("ro.lineage.device", "") == "wisdom";
+}
+
+static bool legacyKernelWithoutNetdBpf() {
+    return isWisdomLegacyKernelCompat() &&
+           access("/sys/fs/bpf/netd_shared/prog_netd_skfilter_ingress_xtbpf", F_OK) != 0;
+}
+
 /**
  * List of module chains to be created, along with explicit ordering. ORDERING
  * IS CRITICAL, AND SHOULD BE TRIPLE-CHECKED WITH EACH CHANGE.
@@ -321,12 +337,16 @@ void Controllers::init() {
 
     if (int ret = bandwidthCtrl.enableBandwidthControl()) {
         gLog.error("Failed to initialize BandwidthController (%s)", strerror(-ret));
-        // A failure to init almost definitely means that iptables failed to load
-        // our static ruleset, which then basically means network accounting will not work.
-        // As such simply exit netd.  This may crash loop the system, but by failing
-        // to bootup we will trigger rollback and thus this offers us protection against
-        // a mainline update breaking things.
-        exit(1);
+        if (legacyKernelWithoutNetdBpf()) {
+            gLog.warn("Continuing without BandwidthController BPF rules on legacy kernel");
+        } else {
+            // A failure to init almost definitely means that iptables failed to load
+            // our static ruleset, which then basically means network accounting will not work.
+            // As such simply exit netd.  This may crash loop the system, but by failing
+            // to bootup we will trigger rollback and thus this offers us protection against
+            // a mainline update breaking things.
+            exit(1);
+        }
     }
     gLog.info("Enabling bandwidth control: %" PRId64 "us", s.getTimeAndResetUs());
 
