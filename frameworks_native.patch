diff --git a/cmds/servicemanager/Access.cpp b/cmds/servicemanager/Access.cpp
index 6e2abf6..0075f22 100644
--- a/cmds/servicemanager/Access.cpp
+++ b/cmds/servicemanager/Access.cpp
@@ -16,6 +16,8 @@
 
 #include "Access.h"
 
+#include <errno.h>
+
 #include <android-base/logging.h>
 #include <binder/IPCThreadState.h>
 #include <log/log_safetynet.h>
@@ -149,8 +151,16 @@ bool Access::actionAllowed(const CallingContext& sctx, const char* tctx, const c
         .tname = &tname,
     };
 
-    return 0 == selinux_check_access(sctx.sid.c_str(), tctx, tclass, perm,
-        reinterpret_cast<void*>(&data));
+    if (selinux_check_access(sctx.sid.c_str(), tctx, tclass, perm,
+                             reinterpret_cast<void*>(&data)) == 0) {
+        return true;
+    }
+    if (errno == EINVAL) {
+        LOG(WARNING) << "SELinux service_manager class unavailable during boot bring-up; "
+                     << "allowing " << perm << " for " << tname;
+        return true;
+    }
+    return false;
 #else
     (void)sctx;
     (void)tctx;
diff --git a/libs/gui/BLASTBufferQueue.cpp b/libs/gui/BLASTBufferQueue.cpp
index 05fbc14..2bfd407 100644
--- a/libs/gui/BLASTBufferQueue.cpp
+++ b/libs/gui/BLASTBufferQueue.cpp
@@ -28,6 +28,7 @@
 #include <gui/BufferQueueConsumer.h>
 #include <gui/BufferQueueCore.h>
 #include <gui/BufferQueueProducer.h>
+#include <climits>
 #include <sys/epoll.h>
 #include <sys/eventfd.h>
 
@@ -77,6 +78,33 @@ timespec timespecFromNanos(nsecs_t duration) {
     return result;
 }
 
+int epollWaitWithFallback(int epollFd, epoll_event* event, nsecs_t timeout) {
+    std::optional<timespec> timespec;
+    if (timeout >= 0) {
+        timespec = timespecFromNanos(timeout);
+    }
+
+    int eventCount;
+    do {
+        eventCount = epoll_pwait2(epollFd, event, 1 /*maxevents*/,
+                                  timespec ? &(*timespec) : nullptr, nullptr /*sigmask*/);
+    } while (eventCount == -1 && errno == EINTR);
+
+    if (eventCount != -1 || errno != ENOSYS) {
+        return eventCount;
+    }
+
+    const int timeoutMillis = timeout < 0
+            ? -1
+            : static_cast<int>(std::min<int64_t>((timeout + 999'999) / 1'000'000, INT_MAX));
+    do {
+        eventCount = epoll_pwait(epollFd, event, 1 /*maxevents*/, timeoutMillis,
+                                 nullptr /*sigmask*/);
+    } while (eventCount == -1 && errno == EINTR);
+
+    return eventCount;
+}
+
 } // namespace
 
 namespace android {
@@ -1459,17 +1487,8 @@ BufferReleaseReader::BufferReleaseReader(
 
 status_t BufferReleaseReader::readBlocking(ReleaseCallbackId& outId, sp<Fence>& outFence,
                                            uint32_t& outMaxAcquiredBufferCount, nsecs_t timeout) {
-    std::optional<timespec> timespec;
-    if (timeout >= 0) {
-        timespec = timespecFromNanos(timeout);
-    }
-
     epoll_event event{};
-    int eventCount;
-    do {
-        eventCount = epoll_pwait2(mEpollFd.get(), &event, 1 /*maxevents*/,
-                                  timespec ? &(*timespec) : nullptr, nullptr /*sigmask*/);
-    } while (eventCount == -1 && errno == EINTR);
+    int eventCount = epollWaitWithFallback(mEpollFd.get(), &event, timeout);
 
     if (eventCount == -1) {
         ALOGE("epoll_wait error while waiting for buffer release. errno=%d message='%s'", errno,
diff --git a/libs/ui/GraphicBufferMapper.cpp b/libs/ui/GraphicBufferMapper.cpp
index 7b5a27d..f8d893f 100644
--- a/libs/ui/GraphicBufferMapper.cpp
+++ b/libs/ui/GraphicBufferMapper.cpp
@@ -39,6 +39,7 @@
 #include <ui/Gralloc5.h>
 #include <ui/GraphicBuffer.h>
 
+#include <hardware/gralloc1.h>
 #include <system/graphics.h>
 
 using unique_fd = ::android::base::unique_fd;
@@ -50,6 +51,17 @@ using LockResult = GraphicBufferMapper::LockResult;
 
 ANDROID_SINGLETON_STATIC_INSTANCE( GraphicBufferMapper )
 
+static uint64_t convertLegacyLockUsage(uint64_t producerUsage, uint64_t consumerUsage) {
+    uint32_t usage = ANDROID_NATIVE_UNSIGNED_CAST(
+            android_convertGralloc1To0Usage(producerUsage, consumerUsage));
+    if ((producerUsage & GRALLOC1_PRODUCER_USAGE_VIDEO_DECODER) != 0 &&
+            (usage & GRALLOC_USAGE_SW_READ_MASK) == 0 &&
+            (usage & GRALLOC_USAGE_PROTECTED) == 0) {
+        usage |= GRALLOC_USAGE_SW_READ_OFTEN;
+    }
+    return usage & (GRALLOC_USAGE_SW_READ_MASK | GRALLOC_USAGE_SW_WRITE_MASK);
+}
+
 void GraphicBufferMapper::preloadHal() {
     Gralloc2Mapper::preload();
     Gralloc3Mapper::preload();
@@ -179,6 +191,16 @@ status_t GraphicBufferMapper::unlock(buffer_handle_t handle, base::unique_fd* ou
     return OK;
 }
 
+extern "C" __attribute__((visibility("default"))) status_t
+_ZN7android19GraphicBufferMapper6unlockEPK13native_handle(GraphicBufferMapper* mapper,
+                                                          buffer_handle_t handle) {
+    if (!mapper) {
+        return BAD_VALUE;
+    }
+
+    return mapper->unlock(handle, nullptr);
+}
+
 status_t GraphicBufferMapper::lock(buffer_handle_t handle, uint32_t usage, const Rect& bounds,
                                    void** vaddr) {
     auto result = lock(handle, static_cast<int64_t>(usage), bounds);
@@ -208,10 +230,31 @@ status_t GraphicBufferMapper::lockAsync(buffer_handle_t handle, uint32_t usage,
 status_t GraphicBufferMapper::lockAsync(buffer_handle_t handle, uint64_t producerUsage,
                                         uint64_t consumerUsage, const Rect& bounds, void** vaddr,
                                         int fenceFd) {
-    return lockAsync(handle,
-                     ANDROID_NATIVE_UNSIGNED_CAST(
-                             android_convertGralloc1To0Usage(producerUsage, consumerUsage)),
-                     bounds, vaddr, fenceFd);
+    return lockAsync(handle, convertLegacyLockUsage(producerUsage, consumerUsage), bounds, vaddr,
+                     fenceFd);
+}
+
+status_t GraphicBufferMapper::lockAsync(buffer_handle_t handle, uint64_t producerUsage,
+                                        uint64_t consumerUsage, const Rect& bounds, void** vaddr,
+                                        int fenceFd, int* outBytesPerPixel,
+                                        int* outBytesPerStride) {
+    ATRACE_CALL();
+
+    int32_t bytesPerPixel = -1;
+    int32_t bytesPerStride = -1;
+    const uint64_t usage = convertLegacyLockUsage(producerUsage, consumerUsage);
+    ALOGV("legacy lockAsync usage producer=0x%" PRIx64 " consumer=0x%" PRIx64
+          " cpu=0x%" PRIx64,
+          producerUsage, consumerUsage, usage);
+    status_t err = mMapper->lock(handle, usage, bounds, fenceFd, vaddr,
+                                 &bytesPerPixel, &bytesPerStride);
+    if (outBytesPerPixel) {
+        *outBytesPerPixel = bytesPerPixel;
+    }
+    if (outBytesPerStride) {
+        *outBytesPerStride = bytesPerStride;
+    }
+    return err;
 }
 
 status_t GraphicBufferMapper::lockAsyncYCbCr(buffer_handle_t handle, uint32_t usage,
diff --git a/libs/ui/include/ui/GraphicBufferMapper.h b/libs/ui/include/ui/GraphicBufferMapper.h
index 91aabe9..6d334d1 100644
--- a/libs/ui/include/ui/GraphicBufferMapper.h
+++ b/libs/ui/include/ui/GraphicBufferMapper.h
@@ -105,6 +105,10 @@ public:
     status_t lockAsync(buffer_handle_t handle, uint64_t producerUsage, uint64_t consumerUsage,
                        const Rect& bounds, void** vaddr, int fenceFd);
 
+    status_t lockAsync(buffer_handle_t handle, uint64_t producerUsage, uint64_t consumerUsage,
+                       const Rect& bounds, void** vaddr, int fenceFd, int* outBytesPerPixel,
+                       int* outBytesPerStride);
+
     status_t lockAsyncYCbCr(buffer_handle_t handle,
             uint32_t usage, const Rect& bounds, android_ycbcr *ycbcr,
             int fenceFd);
@@ -191,4 +195,3 @@ private:
 } // namespace android
 
 #endif // ANDROID_UI_BUFFER_MAPPER_H
-
