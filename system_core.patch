diff --git a/libprocessgroup/processgroup.cpp b/libprocessgroup/processgroup.cpp
index 95221594b..300106557 100644
--- a/libprocessgroup/processgroup.cpp
+++ b/libprocessgroup/processgroup.cpp
@@ -711,8 +711,17 @@ int createProcessGroup(uid_t uid, pid_t initialPid, bool memControl) {
     }
 
     std::string cgroup;
-    CgroupGetControllerPath(CGROUPV2_HIERARCHY_NAME, &cgroup);
-    return createProcessGroupInternal(uid, initialPid, cgroup, true);
+    if (CgroupGetControllerPath(CGROUPV2_HIERARCHY_NAME, &cgroup) &&
+        access((cgroup + "/cgroup.controllers").c_str(), F_OK) == 0) {
+        return createProcessGroupInternal(uid, initialPid, cgroup, true);
+    }
+
+    if (CgroupGetMemcgAppsPath(&cgroup) && access(cgroup.c_str(), F_OK) == 0) {
+        return createProcessGroupInternal(uid, initialPid, cgroup, false);
+    }
+
+    LOG(ERROR) << "Process cgroup controller is not mounted.";
+    return -ENOENT;
 }
 
 static bool SetProcessGroupValue(pid_t tid, const std::string& attr_name, int64_t value) {
diff --git a/libprocessgroup/profiles/cgroups.json b/libprocessgroup/profiles/cgroups.json
index 3e4393df2..82a5e3ec0 100644
--- a/libprocessgroup/profiles/cgroups.json
+++ b/libprocessgroup/profiles/cgroups.json
@@ -35,6 +35,7 @@
     "Mode": "0775",
     "UID": "system",
     "GID": "system",
+    "Optional": true,
     "Controllers": [
       {
         "Controller": "freezer",
diff --git a/rootdir/init.rc b/rootdir/init.rc
index 0cc129707..98230e801 100644
--- a/rootdir/init.rc
+++ b/rootdir/init.rc
@@ -704,7 +704,8 @@ on post-fs-data
     # We restorecon /data in case the userdata partition has been reset.
     restorecon /data
 
-    # Make sure we have the device encryption key.
+    # FBE requires vold to create /data/unencrypted/ref and mode before init
+    # creates encrypted /data subdirectories below.
     installkey /data
 
     # Start bootcharting as soon as possible after the data partition is
@@ -1126,7 +1127,11 @@ on property:vold.checkpoint_committed=1
 # stuff) and we absolutely want to be before netd and the system boot slot is
 # considered to have booted successfully.
 on load-bpf-programs
-    exec_start bpfloader
+    # SM-P205/wisdom is temporarily booting LOS 22.2 with a legacy 4.4
+    # prebuilt kernel, where Android 15 mainline netbpfload cannot succeed.
+    # Skip it for minimal boot bring-up and unblock netd's BPF readiness wait.
+    setprop bpf.progs_loaded 1
+    # exec_start bpfloader
 
 on bpf-progs-loaded
     start netd
