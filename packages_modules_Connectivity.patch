diff --git a/bpf/loader/NetBpfLoad.cpp b/bpf/loader/NetBpfLoad.cpp
index 2388647..d9c6b4c 100644
--- a/bpf/loader/NetBpfLoad.cpp
+++ b/bpf/loader/NetBpfLoad.cpp
@@ -135,6 +135,19 @@ inline bool isUserdebug() {
     return getBuildType() == "userdebug";
 }
 
+static bool isWisdomLegacyKernelCompat() {
+    return GetProperty("ro.boot.hardware", "") == "exynos7904" ||
+           GetProperty("ro.hardware", "") == "exynos7904" ||
+           GetProperty("ro.product.device", "") == "wisdom" ||
+           GetProperty("ro.product.device", "") == "p205" ||
+           GetProperty("ro.lineage.device", "") == "wisdom";
+}
+
+static unsigned int bpfProgramKernelVersion() {
+    if (isWisdomLegacyKernelCompat()) return KVER(4, 9, 0);
+    return kernelVer;
+}
+
 static unsigned int page_size = static_cast<unsigned int>(getpagesize());
 
 typedef struct {
@@ -786,8 +799,18 @@ static int pinMap(const borrowed_fd& fd, const struct bpf_map_def& mapDef) {
         return 0;
 }
 
+static bool isWisdomBackportedMapType(enum bpf_map_type type) {
+    return isWisdomLegacyKernelCompat() && type == BPF_MAP_TYPE_LPM_TRIE;
+}
+
+static bool isKernelVersionInRangeForMap(const struct bpf_map_def& mapDef) {
+    if (kernelVer < mapDef.min_kver) return isWisdomBackportedMapType(mapDef.type);
+    return kernelVer < mapDef.max_kver;
+}
+
 static bool isMapTypeSupported(enum bpf_map_type type) {
-    if (type == BPF_MAP_TYPE_LPM_TRIE && !isAtLeastKernelVersion(4, 14)) {
+    if (type == BPF_MAP_TYPE_LPM_TRIE && !isAtLeastKernelVersion(4, 14) &&
+        !isWisdomBackportedMapType(type)) {
         // On Linux Kernels older than 4.14 this map type doesn't exist - autoskip.
         return false;
     }
@@ -856,7 +879,7 @@ static int createMaps(ElfObject& elfObj, vector<struct bpf_map_def>& md, vector<
             continue;
         }
 
-        if (kernelVer < md[i].min_kver) {
+        if (kernelVer < md[i].min_kver && !isKernelVersionInRangeForMap(md[i])) {
             ALOGD("skipping map %s which requires kernel version 0x%x >= 0x%x",
                   md[i].name(), kernelVer, md[i].min_kver);
             mapFds.push_back(unique_fd());
@@ -870,6 +893,11 @@ static int createMaps(ElfObject& elfObj, vector<struct bpf_map_def>& md, vector<
             continue;
         }
 
+        if (kernelVer < md[i].min_kver) {
+            ALOGW("loading wisdom backported map %s type %d on kernel 0x%x below declared 0x%x",
+                  md[i].name(), md[i].type, kernelVer, md[i].min_kver);
+        }
+
         if (!isMapTypeSupported(md[i].type)) {
             ALOGD("skipping unsupported map type(%d): %s", md[i].type, md[i].name());
             mapFds.push_back(unique_fd());
@@ -1058,6 +1086,10 @@ static int validateProg(const borrowed_fd& fd, const char* const progPinLoc) {
     ALOGI("prog %s id %d len jit:%d xlat:%d", progPinLoc, progId, jitLen, xlatLen);
 
     if (!jitLen && api_level_full >= BPFLOADER_MAINLINE_25Q2_VERSION) {
+        if (isWisdomLegacyKernelCompat()) {
+            ALOGW("Allowing wisdom legacy kernel eBPF interpreter for %s", progPinLoc);
+            return 0;
+        }
         ALOGE("Kernel eBPF JIT failure for %s", progPinLoc);
         return -ENOTSUP;
     }
@@ -1065,6 +1097,8 @@ static int validateProg(const borrowed_fd& fd, const char* const progPinLoc) {
 }
 
 static int loadCodeSections(ElfObject& elfObj, vector<codeSection>& cs, const string& license) {
+    const unsigned progKernelVer = bpfProgramKernelVersion();
+
     for (int i = 0; i < (int)cs.size(); i++) {
         unique_fd& fd = cs[i].prog_fd;
         int ret;
@@ -1079,8 +1113,8 @@ static int loadCodeSections(ElfObject& elfObj, vector<codeSection>& cs, const st
               cs[i].prog_def->min_kver, cs[i].prog_def->max_kver,
               cs[i].prog_def->bpfloader_min_ver, cs[i].prog_def->bpfloader_max_ver);
 
-        if (kernelVer < cs[i].prog_def->min_kver) continue;
-        if (kernelVer >= cs[i].prog_def->max_kver) continue;
+        if (progKernelVer < cs[i].prog_def->min_kver) continue;
+        if (progKernelVer >= cs[i].prog_def->max_kver) continue;
         if (api_level_full < cs[i].prog_def->bpfloader_min_ver) continue;
         if (api_level_full >= cs[i].prog_def->bpfloader_max_ver) continue;
 
@@ -1168,13 +1202,18 @@ static int prepareLoadMaps(const struct bpf_object* obj, const vector<struct bpf
             continue;
         }
 
-        if (kernelVer < md[i].min_kver || kernelVer >= md[i].max_kver) {
+        if (!isKernelVersionInRangeForMap(md[i])) {
             ALOGD("skipping map %s: kernel version 0x%x is outside required range [0x%x, 0x%x)",
                   md[i].name(), kernelVer, md[i].min_kver, md[i].max_kver);
             bpf_map__set_autocreate(m, false);
             continue;
         }
 
+        if (kernelVer < md[i].min_kver) {
+            ALOGW("loading wisdom backported map %s type %d on kernel 0x%x below declared 0x%x",
+                  md[i].name(), md[i].type, kernelVer, md[i].min_kver);
+        }
+
         if (!isMapTypeSupported(md[i].type)) {
             ALOGD("skipping unsupported map type(%d): %s", md[i].type, md[i].name());
             bpf_map__set_autocreate(m, false);
@@ -1188,6 +1227,8 @@ static int prepareLoadMaps(const struct bpf_object* obj, const vector<struct bpf
 }
 
 static int prepareLoadProgs(const struct bpf_object* obj, const vector<codeSection>& cs) {
+    const unsigned progKernelVer = bpfProgramKernelVersion();
+
     for (int i = 0; i < (int)cs.size(); i++) {
         if (!cs[i].prog_def.has_value()) {
             ALOGE("[%d] missing program definition! bad bpf.o build?", i);
@@ -1202,9 +1243,9 @@ static int prepareLoadProgs(const struct bpf_object* obj, const vector<codeSecti
 
         unsigned min_kver = cs[i].prog_def->min_kver;
         unsigned max_kver = cs[i].prog_def->max_kver;
-        if (kernelVer < min_kver || kernelVer >= max_kver) {
+        if (progKernelVer < min_kver || progKernelVer >= max_kver) {
             ALOGD("skipping prog %s: kernel version 0x%x is outside required range [0x%x, 0x%x)",
-                  cs[i].prog_def->name(), kernelVer, min_kver, max_kver);
+                  cs[i].prog_def->name(), progKernelVer, min_kver, max_kver);
             bpf_program__set_autoload(prog, false);
             continue;
         }
@@ -1578,6 +1619,7 @@ static int doLoad(char** argv, char * const envp[]) {
     libbpf_set_print(libbpfPrint);
 
     const bool runningAsRoot = !getuid();  // true iff U QPR3 or V+
+    const bool allow_legacy_kernel_compat = isWisdomLegacyKernelCompat();
 
     const int first_api_level = GetIntProperty("ro.board.first_api_level", api_level_full / 100);
 
@@ -1607,28 +1649,40 @@ static int doLoad(char** argv, char * const envp[]) {
     // both S and T require kernel 4.9 (and eBpf support)
     // (this also guarantees 'kernelVer' isn't an invalid uninitialized 0)
     if (!isAtLeastKernelVersion(4, 9)) {
-        ALOGE("Android S & T require kernel 4.9.");
-        return 3;
+        if (!allow_legacy_kernel_compat) {
+            ALOGE("Android S & T require kernel 4.9.");
+            return 3;
+        }
+        ALOGW("Allowing wisdom legacy kernel <4.9; relying on runtime eBPF capability checks.");
     }
 
     // U bumps the kernel requirement up to 4.14
     if (isAtLeastU && !isAtLeastKernelVersion(4, 14)) {
-        ALOGE("Android U requires kernel 4.14.");
-        return 4;
+        if (!allow_legacy_kernel_compat) {
+            ALOGE("Android U requires kernel 4.14.");
+            return 4;
+        }
+        ALOGW("Allowing wisdom legacy kernel <4.14 during eBPF bring-up.");
     }
 
     // V bumps the kernel requirement up to 4.19
     // see also: //system/netd/tests/kernel_test.cpp TestKernel419
     if (isAtLeastV && !isAtLeastKernelVersion(4, 19)) {
-        ALOGE("Android V requires kernel 4.19.");
-        return 5;
+        if (!allow_legacy_kernel_compat) {
+            ALOGE("Android V requires kernel 4.19.");
+            return 5;
+        }
+        ALOGW("Allowing wisdom legacy kernel <4.19 during eBPF bring-up.");
     }
 
     // 25Q2 bumps the kernel requirement up to 5.4
     // see also: //system/netd/tests/kernel_test.cpp TestKernel54
     if (isAtLeast25Q2 && !isAtLeastKernelVersion(5, 4)) {
-        ALOGE("Android 25Q2 requires kernel 5.4.");
-        return 6;
+        if (!allow_legacy_kernel_compat) {
+            ALOGE("Android 25Q2 requires kernel 5.4.");
+            return 6;
+        }
+        ALOGW("Allowing wisdom legacy kernel <5.4 during eBPF bring-up.");
     }
 
     // 25Q4 bumps the kernel requirement up to 5.10
@@ -1819,7 +1873,7 @@ static int doLoad(char** argv, char * const envp[]) {
         if (!createDir("/sys/fs/bpf/loader")) return 31;
     }
 
-    if (runningAsRoot) {  // implies U QPR3+ and kernel 4.14+
+    if (runningAsRoot && isAtLeastKernelVersion(4, 14)) {
         // There should not be any programs or maps yet
         errno = 0;
         uint32_t progId = bpfGetNextProgId(0);  // expect 0 with errno == ENOENT
diff --git a/bpf/loader/netbpfload.31rc b/bpf/loader/netbpfload.31rc
index bca7dc8..1e60015 100644
--- a/bpf/loader/netbpfload.31rc
+++ b/bpf/loader/netbpfload.31rc
@@ -10,4 +10,4 @@ service mdnsd_netbpfload /apex/com.android.tethering/bin/netbpfload
     user system
     rlimit memlock 1073741824 1073741824
     oneshot
-    reboot_on_failure reboot,netbpfload-failed
+    reboot_on_failure reboot,recovery
diff --git a/bpf/loader/netbpfload.33rc b/bpf/loader/netbpfload.33rc
index eb937dd..4ba72e0 100644
--- a/bpf/loader/netbpfload.33rc
+++ b/bpf/loader/netbpfload.33rc
@@ -17,4 +17,4 @@ service mdnsd_netbpfload /apex/com.android.tethering/bin/netbpfload
     file /dev/kmsg w
     rlimit memlock 1073741824 1073741824
     oneshot
-    reboot_on_failure reboot,netbpfload-failed
+    reboot_on_failure reboot,recovery
diff --git a/bpf/loader/netbpfload.35rc b/bpf/loader/netbpfload.35rc
index a23bd60..80d0bc6 100644
--- a/bpf/loader/netbpfload.35rc
+++ b/bpf/loader/netbpfload.35rc
@@ -65,5 +65,5 @@ service bpfloader /apex/com.android.tethering/bin/netbpfload
     #    'cannot prove return value is 0 or 1' or 'unsupported / unknown operation / helper',
     #    'invalid bpf_context access', etc.
     #
-    reboot_on_failure reboot,bpfloader-failed
+    reboot_on_failure reboot,recovery
     override
diff --git a/bpf/loader/netbpfload.rc b/bpf/loader/netbpfload.rc
index 0430dfe..f209da3 100644
--- a/bpf/loader/netbpfload.rc
+++ b/bpf/loader/netbpfload.rc
@@ -12,5 +12,5 @@
 service bpfloader /system/bin/false
     user root
     oneshot
-    reboot_on_failure reboot,netbpfload-missing
+    reboot_on_failure reboot,recovery
     updatable
diff --git a/bpf/netd/BpfHandler.cpp b/bpf/netd/BpfHandler.cpp
index 7367ec5..f328eeb 100644
--- a/bpf/netd/BpfHandler.cpp
+++ b/bpf/netd/BpfHandler.cpp
@@ -21,12 +21,15 @@
 #include <linux/bpf.h>
 #include <inttypes.h>
 
+#include <android-base/properties.h>
 #include <android-base/unique_fd.h>
 #include <bpf/WaitForProgsLoaded.h>
 #include <log/log.h>
 #include <netdutils/UidConstants.h>
 #include <private/android_filesystem_config.h>
 
+#include <string>
+
 #include "BpfSyscallWrappers.h"
 
 namespace android {
@@ -55,6 +58,24 @@ constexpr int TOTAL_UID_STATS_ENTRIES_LIMIT = STATS_MAP_SIZE * 0.9;
 static_assert(STATS_MAP_SIZE - TOTAL_UID_STATS_ENTRIES_LIMIT > 100,
               "The limit for stats map is to high, stats data may be lost due to overflow");
 
+static bool isWisdomLegacyKernelCompat() {
+    return base::GetProperty("ro.boot.hardware", "") == "exynos7904" ||
+           base::GetProperty("ro.hardware", "") == "exynos7904" ||
+           base::GetProperty("ro.product.device", "") == "wisdom" ||
+           base::GetProperty("ro.product.device", "") == "p205" ||
+           base::GetProperty("ro.lineage.device", "") == "wisdom";
+}
+
+static bool isAtLeastNetdBpfKernelVersion(unsigned major, unsigned minor) {
+    if (!isWisdomLegacyKernelCompat()) return isAtLeastKernelVersion(major, minor);
+
+    // The wisdom kernel reports a newer compatibility version, but the netd BPF
+    // surface we can rely on during bring-up is the 4.9-era subset.
+    if (major < 4) return true;
+    if (major > 4) return false;
+    return minor <= 9;
+}
+
 static Status attachProgramToCgroup(const char* programPath, const unique_fd& cgroupFd,
                                     bpf_attach_type type) {
     unique_fd cgroupProg(retrieveProgram(programPath));
@@ -77,18 +98,22 @@ static Status checkProgramAccessible(const char* programPath) {
 
 static Status initPrograms(const char* cg2_path) {
     if (!cg2_path) return Status("cg2_path is NULL");
+    const bool wisdomLegacyKernelCompat = isWisdomLegacyKernelCompat();
 
     // This code was mainlined in T, so this should be trivially satisfied.
     if (!isAtLeastT) return Status("S- platform is unsupported");
 
     // S requires eBPF support which was only added in 4.9, so this should be satisfied.
-    if (!isAtLeastKernelVersion(4, 9)) {
+    if (!isAtLeastNetdBpfKernelVersion(4, 9)) {
         return Status("kernel version < 4.9.0 is unsupported");
     }
 
     // U bumps the kernel requirement up to 4.14
-    if (isAtLeastU && !isAtLeastKernelVersion(4, 14)) {
-        return Status("U+ platform with kernel version < 4.14.0 is unsupported");
+    if (isAtLeastU && !isAtLeastNetdBpfKernelVersion(4, 14)) {
+        if (!wisdomLegacyKernelCompat) {
+            return Status("U+ platform with kernel version < 4.14.0 is unsupported");
+        }
+        ALOGW("Allowing wisdom legacy netd BPF mode below Android U kernel requirements.");
     }
 
     // U mandates this mount point (though it should also be the case on T)
@@ -97,13 +122,19 @@ static Status initPrograms(const char* cg2_path) {
     }
 
     // V bumps the kernel requirement up to 4.19
-    if (isAtLeastV && !isAtLeastKernelVersion(4, 19)) {
-        return Status("V+ platform with kernel version < 4.19.0 is unsupported");
+    if (isAtLeastV && !isAtLeastNetdBpfKernelVersion(4, 19)) {
+        if (!wisdomLegacyKernelCompat) {
+            return Status("V+ platform with kernel version < 4.19.0 is unsupported");
+        }
+        ALOGW("Allowing wisdom legacy netd BPF mode below Android V kernel requirements.");
     }
 
     // 25Q2 bumps the kernel requirement up to 5.4
-    if (isAtLeast25Q2 && !isAtLeastKernelVersion(5, 4)) {
-        return Status("25Q2+ platform with kernel version < 5.4.0 is unsupported");
+    if (isAtLeast25Q2 && !isAtLeastNetdBpfKernelVersion(5, 4)) {
+        if (!wisdomLegacyKernelCompat) {
+            return Status("25Q2+ platform with kernel version < 5.4.0 is unsupported");
+        }
+        ALOGW("Allowing wisdom legacy netd BPF mode below Android 25Q2 kernel requirements.");
     }
 
     unique_fd cg_fd(open(cg2_path, O_DIRECTORY | O_RDONLY | O_CLOEXEC));
@@ -121,12 +152,12 @@ static Status initPrograms(const char* cg2_path) {
     // cgroup if the program is pinned properly.
     // TODO: delete the if statement once all devices should support cgroup
     // socket filter (ie. the minimum kernel version required is 4.14).
-    if (isAtLeastKernelVersion(4, 14)) {
+    if (isAtLeastNetdBpfKernelVersion(4, 14)) {
         RETURN_IF_NOT_OK(attachProgramToCgroup(CGROUP_INET_CREATE_PROG_PATH,
                                     cg_fd, BPF_CGROUP_INET_SOCK_CREATE));
     }
 
-    if (isAtLeastKernelVersion(5, 10)) {
+    if (isAtLeastNetdBpfKernelVersion(5, 10)) {
         RETURN_IF_NOT_OK(attachProgramToCgroup(CGROUP_INET_RELEASE_PROG_PATH,
                                     cg_fd, BPF_CGROUP_INET_SOCK_RELEASE));
     }
@@ -134,7 +165,7 @@ static Status initPrograms(const char* cg2_path) {
     if (isAtLeastV) {
         // V requires 4.19+, so technically this 2nd 'if' is not required, but it
         // doesn't hurt us to try to support AOSP forks that try to support older kernels.
-        if (isAtLeastKernelVersion(4, 19)) {
+        if (isAtLeastNetdBpfKernelVersion(4, 19)) {
             RETURN_IF_NOT_OK(attachProgramToCgroup(CGROUP_CONNECT4_PROG_PATH,
                                         cg_fd, BPF_CGROUP_INET4_CONNECT));
             RETURN_IF_NOT_OK(attachProgramToCgroup(CGROUP_CONNECT6_PROG_PATH,
@@ -149,7 +180,7 @@ static Status initPrograms(const char* cg2_path) {
                                         cg_fd, BPF_CGROUP_UDP6_SENDMSG));
         }
 
-        if (isAtLeastKernelVersion(5, 4)) {
+        if (isAtLeastNetdBpfKernelVersion(5, 4)) {
             RETURN_IF_NOT_OK(attachProgramToCgroup(CGROUP_GETSOCKOPT_PROG_PATH,
                                         cg_fd, BPF_CGROUP_GETSOCKOPT));
             RETURN_IF_NOT_OK(attachProgramToCgroup(CGROUP_SETSOCKOPT_PROG_PATH,
@@ -157,7 +188,7 @@ static Status initPrograms(const char* cg2_path) {
         }
     }
 
-    if (isAtLeastKernelVersion(4, 19)) {
+    if (isAtLeastNetdBpfKernelVersion(4, 19)) {
         RETURN_IF_NOT_OK(attachProgramToCgroup(CGROUP_BIND4_PROG_PATH,
                 cg_fd, BPF_CGROUP_INET4_BIND));
         RETURN_IF_NOT_OK(attachProgramToCgroup(CGROUP_BIND6_PROG_PATH,
@@ -172,14 +203,14 @@ static Status initPrograms(const char* cg2_path) {
         if (queryProgram(cg_fd, BPF_CGROUP_INET6_BIND) <= 0) abort();
     }
 
-    if (isAtLeastKernelVersion(5, 10)) {
+    if (isAtLeastNetdBpfKernelVersion(5, 10)) {
         if (queryProgram(cg_fd, BPF_CGROUP_INET_SOCK_RELEASE) <= 0) abort();
     }
 
     if (isAtLeastV) {
         // V requires 4.19+, so technically this 2nd 'if' is not required, but it
         // doesn't hurt us to try to support AOSP forks that try to support older kernels.
-        if (isAtLeastKernelVersion(4, 19)) {
+        if (isAtLeastNetdBpfKernelVersion(4, 19)) {
             if (queryProgram(cg_fd, BPF_CGROUP_INET4_CONNECT) <= 0) abort();
             if (queryProgram(cg_fd, BPF_CGROUP_INET6_CONNECT) <= 0) abort();
             if (queryProgram(cg_fd, BPF_CGROUP_UDP4_RECVMSG) <= 0) abort();
@@ -188,7 +219,7 @@ static Status initPrograms(const char* cg2_path) {
             if (queryProgram(cg_fd, BPF_CGROUP_UDP6_SENDMSG) <= 0) abort();
         }
 
-        if (isAtLeastKernelVersion(5, 4)) {
+        if (isAtLeastNetdBpfKernelVersion(5, 4)) {
             if (queryProgram(cg_fd, BPF_CGROUP_GETSOCKOPT) <= 0) abort();
             if (queryProgram(cg_fd, BPF_CGROUP_SETSOCKOPT) <= 0) abort();
         }
@@ -258,6 +289,12 @@ static inline void waitForBpf() {
 }
 
 Status BpfHandler::init(const char* cg2_path) {
+    if (isWisdomLegacyKernelCompat() && !isAtLeastKernelVersion(4, 9)) {
+        ALOGW("Enabling wisdom legacy netd mode on kernel < 4.9; BPF socket tagging disabled");
+        mLegacyKernelMode = true;
+        return netdutils::status::ok;
+    }
+
     // This wait is effectively a no-op on U QPR3+ devices (as netd starts
     // *after* the synchronous 'exec_start bpfloader' which calls NetBpfLoad)
     // but checking for U QPR3 is hard.
@@ -271,9 +308,16 @@ Status BpfHandler::init(const char* cg2_path) {
     if (!isAtLeast25Q2) waitForBpf();
 
     RETURN_IF_NOT_OK(initPrograms(cg2_path));
+
+    if (isWisdomLegacyKernelCompat()) {
+        ALOGW("Enabling wisdom legacy netd BPF map mode; socket tagging disabled.");
+        mLegacyKernelMode = true;
+        return netdutils::status::ok;
+    }
+
     RETURN_IF_NOT_OK(initMaps());
 
-    if (isAtLeast25Q2) {
+    if (isAtLeast25Q2 && !isWisdomLegacyKernelCompat()) {
         struct rlimit limit = {
             .rlim_cur = 1u << 30,  // 1 GiB
             .rlim_max = 1u << 30,  // 1 GiB
@@ -319,7 +363,7 @@ static void mapLockTest(void) {
 
 Status BpfHandler::initMaps() {
     // bpfLock() requires bpfGetFdMapId which is only available on 4.14+ kernels.
-    if (isAtLeastKernelVersion(4, 14)) {
+    if (isAtLeastNetdBpfKernelVersion(4, 14)) {
         mapLockTest();
     }
 
@@ -345,6 +389,8 @@ bool BpfHandler::hasUpdateDeviceStatsPermission(uid_t uid) {
 }
 
 int BpfHandler::tagSocket(int sockFd, uint32_t tag, uid_t chargeUid, uid_t realUid) {
+    if (mLegacyKernelMode) return 0;
+
     if (!mCookieTagMap.isValid()) return -EPERM;
 
     if (chargeUid != realUid && !hasUpdateDeviceStatsPermission(realUid)) return -EPERM;
@@ -378,7 +424,7 @@ int BpfHandler::tagSocket(int sockFd, uint32_t tag, uid_t chargeUid, uid_t realU
 
     // On 5.10+ the BPF_CGROUP_INET_SOCK_RELEASE hook takes care of cookie tag map cleanup
     // during socket destruction. As such the socket destroy listener is superfluous.
-    if (!isAtLeastKernelVersion(5, 10)) {
+    if (!isAtLeastNetdBpfKernelVersion(5, 10)) {
         int socketProto;
         socklen_t protoLen = sizeof(socketProto);
         if (getsockopt(sockFd, SOL_SOCKET, SO_PROTOCOL, &socketProto, &protoLen)) {
@@ -451,6 +497,8 @@ int BpfHandler::tagSocket(int sockFd, uint32_t tag, uid_t chargeUid, uid_t realU
 }
 
 int BpfHandler::untagSocket(int sockFd) {
+    if (mLegacyKernelMode) return 0;
+
     uint64_t sock_cookie = getSocketCookie(sockFd);
     if (!sock_cookie) return -errno;
 
diff --git a/bpf/netd/BpfHandler.h b/bpf/netd/BpfHandler.h
index b601b53..aeabb8d 100644
--- a/bpf/netd/BpfHandler.h
+++ b/bpf/netd/BpfHandler.h
@@ -64,6 +64,7 @@ class BpfHandler {
     BpfMapRO<StatsKey, StatsValue> mStatsMapB;
     BpfMapRO<uint32_t, uint32_t> mConfigurationMap;
     BpfMapRO<uint32_t, uint8_t> mUidPermissionMap;
+    bool mLegacyKernelMode = false;
 
     // The limit on the number of stats entries a uid can have in the per uid stats map. BpfHandler
     // will block that specific uid from tagging new sockets after the limit is reached.
@@ -78,4 +79,4 @@ class BpfHandler {
 };
 
 }  // namespace net
-}  // namespace android
\ No newline at end of file
+}  // namespace android
diff --git a/bpf/syscall_wrappers/include/BpfSyscallWrappers.h b/bpf/syscall_wrappers/include/BpfSyscallWrappers.h
index 401c92b..ca31aa5 100644
--- a/bpf/syscall_wrappers/include/BpfSyscallWrappers.h
+++ b/bpf/syscall_wrappers/include/BpfSyscallWrappers.h
@@ -192,7 +192,11 @@ inline int bpfLock(int fd, short type) {
     int saved_errno = errno;
     // 4.14+ required to fetch map id, but we don't want to call isAtLeastKernelVersion
     if (mapId == -1 && saved_errno == EINVAL) return fd;
-    if (mapId <= 0) abort();  // should not be possible
+    // Some legacy vendor kernels implement BPF_OBJ_GET_INFO_BY_FD only
+    // partially and return a zero map id. Treat that like the pre-4.14 case:
+    // keep the map fd usable, but skip userspace OFD map locking.
+    if (!mapId) return fd;
+    if (mapId < 0) abort();  // should not be possible
 
     // on __LP64__ (aka. 64-bit userspace) 'struct flock64' is the same as 'struct flock'
     struct flock64 fl = {
@@ -355,4 +359,3 @@ DEFINE_BPF_GET_FD(prog, XlatProgLen, xlated_prog_len) // int bpfGetFdXlatProgLen
 
 }  // namespace bpf
 }  // namespace android
-
