diff --git a/openmax/component/video/enc/Exynos_OMX_VencControl.c b/openmax/component/video/enc/Exynos_OMX_VencControl.c
index 6292f1e..ffb55d5 100644
--- a/openmax/component/video/enc/Exynos_OMX_VencControl.c
+++ b/openmax/component/video/enc/Exynos_OMX_VencControl.c
@@ -652,7 +652,8 @@ OMX_ERRORTYPE Exynos_OMX_FlushPort(
     if (pExynosPort->bufferProcessType & BUFFER_SHARE) {
         if (pExynosPort->processData.bufferHeader != NULL) {
             if (nPortIndex == INPUT_PORT_INDEX) {
-                if (pExynosPort->eMetaDataType & METADATA_TYPE_BUFFER_LOCK)
+                if ((pExynosPort->eMetaDataType & METADATA_TYPE_BUFFER_LOCK) &&
+                    (pExynosPort->eMetaDataType != METADATA_TYPE_GRAPHIC))
                     Exynos_OSAL_UnlockMetaData(pExynosPort->processData.bufferHeader->pBuffer, pExynosPort->eMetaDataType);
 
                 Exynos_OMX_InputBufferReturn(pOMXComponent, pExynosPort->processData.bufferHeader);
@@ -668,7 +669,8 @@ OMX_ERRORTYPE Exynos_OMX_FlushPort(
                     Exynos_OMX_OutputBufferReturn(pOMXComponent,
                                                   pExynosPort->extendBufferHeader[i].OMXBufferHeader);
                 } else if (nPortIndex == INPUT_PORT_INDEX) {
-                    if (pExynosPort->eMetaDataType & METADATA_TYPE_BUFFER_LOCK)
+                    if ((pExynosPort->eMetaDataType & METADATA_TYPE_BUFFER_LOCK) &&
+                        (pExynosPort->eMetaDataType != METADATA_TYPE_GRAPHIC))
                         Exynos_OSAL_UnlockMetaData(pExynosPort->extendBufferHeader[i].OMXBufferHeader->pBuffer, pExynosPort->eMetaDataType);
 
                     Exynos_OMX_InputBufferReturn(pOMXComponent,
@@ -2602,7 +2604,32 @@ OMX_ERRORTYPE Exynos_Shared_BufferToData(
         /* metadata type */
         EXYNOS_OMX_MULTIPLANE_BUFFER bufferInfo;
 
-        if (pExynosPort->eMetaDataType & METADATA_TYPE_BUFFER_LOCK) {
+        if (pExynosPort->eMetaDataType == METADATA_TYPE_GRAPHIC) {
+            OMX_U32 stride;
+
+            /*
+             * Surface input is consumed by MFC as DMA-BUF.  Do not CPU-lock
+             * legacy camera buffers here: their Exynos multi-plane handle is
+             * rejected by the modern mapper, while the encoder only needs FDs.
+             */
+            stride = pExynosPort->portDefinition.format.video.nFrameWidth;
+            ret = Exynos_OSAL_GetInfoFromGraphicMetaData(pUseBuffer->bufferHeader->pBuffer,
+                                                         &stride, &bufferInfo,
+                                                         pExynosPort->eMetaDataType);
+            if (ret != OMX_ErrorNone) {
+                Exynos_OSAL_Log(EXYNOS_LOG_ERROR, "[%s]: Failed to get graphic metadata (err:0x%x)",
+                                    __FUNCTION__, ret);
+                goto EXIT;
+            }
+
+            /*
+             * The helper uses fd[2] as an address fallback for decoder output.
+             * Encoder components treat addr[2] as ExynosVideoMeta, so discard
+             * that fallback while retaining a genuinely mapped metadata area.
+             */
+            if (bufferInfo.addr[2] == INT_TO_PTR(bufferInfo.fd[2]))
+                bufferInfo.addr[2] = NULL;
+        } else if (pExynosPort->eMetaDataType & METADATA_TYPE_BUFFER_LOCK) {
             EXYNOS_OMX_LOCK_RANGE range;
             OMX_U32 stride;

@@ -2702,7 +2729,8 @@ OMX_ERRORTYPE Exynos_Shared_DataToBuffer(
     pUseBuffer->nFlags                = pData->nFlags;
     pUseBuffer->pPrivate              = pData->pPrivate;
 
-    if (pExynosPort->eMetaDataType & METADATA_TYPE_BUFFER_LOCK)
+    if ((pExynosPort->eMetaDataType & METADATA_TYPE_BUFFER_LOCK) &&
+        (pExynosPort->eMetaDataType != METADATA_TYPE_GRAPHIC))
         Exynos_OSAL_UnlockMetaData(pUseBuffer->bufferHeader->pBuffer, pExynosPort->eMetaDataType);
 
 EXIT:
diff --git a/openmax/osal/Exynos_OSAL_Android.cpp b/openmax/osal/Exynos_OSAL_Android.cpp
index f881b9b..71312ae 100644
--- a/openmax/osal/Exynos_OSAL_Android.cpp
+++ b/openmax/osal/Exynos_OSAL_Android.cpp
@@ -1217,6 +1217,81 @@ EXIT:
     return ret;
 }
 
+OMX_ERRORTYPE Exynos_OSAL_GetInfoFromGraphicMetaData(
+    OMX_IN OMX_PTR                          pBuffer,
+    OMX_OUT OMX_U32                        *pStride,
+    OMX_OUT EXYNOS_OMX_MULTIPLANE_BUFFER   *pBufferInfo,
+    OMX_IN EXYNOS_METADATA_TYPE             eMetaType)
+{
+    OMX_ERRORTYPE ret  = OMX_ErrorNone;
+    OMX_PTR       pBuf = NULL;
+
+    FunctionIn();
+
+    if ((pBuffer == NULL) ||
+        (pStride == NULL) ||
+        (pBufferInfo == NULL)) {
+        Exynos_OSAL_Log(EXYNOS_LOG_ERROR, "[%s] invalid parameter", __FUNCTION__);
+        ret = OMX_ErrorBadParameter;
+        goto EXIT;
+    }
+
+    if (eMetaType == METADATA_TYPE_GRAPHIC) {
+        EXYNOS_OMX_MULTIPLANE_BUFFER bufferInfo;
+
+        ret = Exynos_OSAL_GetInfoFromMetaData(pBuffer, &bufferInfo, eMetaType);
+        if (ret != OMX_ErrorNone) {
+            Exynos_OSAL_Log(EXYNOS_LOG_ERROR, "[%s]: Failed to Exynos_OSAL_GetInfoFromMetaData (err:0x%x)",
+                                __FUNCTION__, ret);
+            goto EXIT;
+        }
+
+        pBuf = bufferInfo.addr[0];
+    } else if (eMetaType == METADATA_TYPE_GRAPHIC_HANDLE) {
+        pBuf = pBuffer;
+    } else {
+        Exynos_OSAL_Log(EXYNOS_LOG_ERROR, "[%s] unsupported metadata type(0x%x)", __FUNCTION__, eMetaType);
+        ret = OMX_ErrorBadParameter;
+        goto EXIT;
+    }
+
+    if (pBuf == NULL) {
+        Exynos_OSAL_Log(EXYNOS_LOG_ERROR, "[%s] graphic buffer handle is NULL", __FUNCTION__);
+        ret = OMX_ErrorBadParameter;
+        goto EXIT;
+    }
+
+    {
+        buffer_handle_t bufferHandle = (buffer_handle_t)pBuf;
+        ExynosGraphicBufferMeta graphicBuffer(bufferHandle);
+
+        Exynos_OSAL_Memset(pBufferInfo, 0, sizeof(EXYNOS_OMX_MULTIPLANE_BUFFER));
+
+        pBufferInfo->fd[0] = graphicBuffer.fd;
+        pBufferInfo->fd[1] = graphicBuffer.fd1;
+        pBufferInfo->fd[2] = graphicBuffer.fd2;
+
+        pBufferInfo->addr[0] = INT_TO_PTR(graphicBuffer.fd);
+        pBufferInfo->addr[1] = INT_TO_PTR(graphicBuffer.fd1);
+        pBufferInfo->addr[2] = graphicBuffer.get_video_metadata(bufferHandle);
+        if ((pBufferInfo->addr[2] == NULL) && (graphicBuffer.fd2 > 0))
+            pBufferInfo->addr[2] = INT_TO_PTR(graphicBuffer.fd2);
+
+        *pStride = (OMX_U32)graphicBuffer.stride;
+        pBufferInfo->eColorFormat = getBufferFormat(pBuf);
+
+        Exynos_OSAL_Log(EXYNOS_LOG_TRACE, "[%s] graphic metadata: handle(%p), FD(%u, %u, %u), stride(%u)",
+                                            __FUNCTION__, pBuf,
+                                            pBufferInfo->fd[0], pBufferInfo->fd[1], pBufferInfo->fd[2],
+                                            *pStride);
+    }
+
+EXIT:
+    FunctionOut();
+
+    return ret;
+}
+
 OMX_ERRORTYPE Exynos_OSAL_UnlockMetaData(
     OMX_IN OMX_PTR              pBuffer,
     OMX_IN EXYNOS_METADATA_TYPE eMetaType)
@@ -1407,8 +1482,7 @@ OMX_ERRORTYPE Exynos_OSAL_RefCount_Increase(
     OMX_COLOR_FORMATTYPE     eColorFormat   = OMX_COLOR_FormatUnused;
 
     buffer_handle_t          bufferHandle  = NULL;
-
-    ExynosGraphicBufferMeta graphicBuffer(bufferHandle);
+    ExynosGraphicBufferMeta  graphicBuffer;
 
     ion_user_handle_t ionHandle  = -1;
     ion_user_handle_t ionHandle1 = -1;
@@ -1440,6 +1514,8 @@ OMX_ERRORTYPE Exynos_OSAL_RefCount_Increase(
         bufferHandle = (buffer_handle_t)bufferInfo.addr[0];
     }
 
+    graphicBuffer.init(bufferHandle);
+
     eColorFormat = Exynos_OSAL_HAL2OMXColorFormat(graphicBuffer.format);
     if (eColorFormat == OMX_COLOR_FormatUnused) {
         Exynos_OSAL_Log(EXYNOS_LOG_ERROR, "[%s] HAL format(0x%x) is invalid", __FUNCTION__, graphicBuffer.format);
diff --git a/openmax/osal/Exynos_OSAL_Platform.h b/openmax/osal/Exynos_OSAL_Platform.h
index 18b0e01..2dad28b 100644
--- a/openmax/osal/Exynos_OSAL_Platform.h
+++ b/openmax/osal/Exynos_OSAL_Platform.h
@@ -58,6 +58,10 @@ OMX_ERRORTYPE Exynos_OSAL_LockMetaData(OMX_IN OMX_PTR pBuffer,
                                        OMX_IN EXYNOS_METADATA_TYPE eMetaType);
 OMX_ERRORTYPE Exynos_OSAL_UnlockMetaData(OMX_IN OMX_PTR pBuffer,
                                          OMX_IN EXYNOS_METADATA_TYPE eMetaType);
+OMX_ERRORTYPE Exynos_OSAL_GetInfoFromGraphicMetaData(OMX_IN OMX_PTR pBuffer,
+                                                     OMX_OUT OMX_U32 *pStride,
+                                                     OMX_OUT EXYNOS_OMX_MULTIPLANE_BUFFER *pBufferInfo,
+                                                     OMX_IN EXYNOS_METADATA_TYPE eMetaType);
 
 OMX_ERRORTYPE Exynos_OSAL_GetInfoFromMetaData(OMX_IN OMX_PTR pBuffer, OMX_OUT EXYNOS_OMX_MULTIPLANE_BUFFER *pBufferInfo, OMX_IN EXYNOS_METADATA_TYPE eMetaDataType);
 
